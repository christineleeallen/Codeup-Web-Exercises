/**
 * Created by home on 11/15/16.
 */
var bucketList = ['Rappel into a cave', 'Take a falconry class', 'Learn to juggle'];

var listItem = bucketList[0];
console.log(listItem);

var listItem = bucketList[2];
console.log(listItem);

var listItem = bucketList[3];
console.log(listItem);

var hello = 'Hello World';
console.log(hello[6]);

var bucketList = ['Rappel into a cave', 'Take a falconry class', 'Learn to juggle'];
console.log(bucketList.length);

var hello = 'Hello World';
console.log('Hello World'.length);


//Rappel into a cave
//Learn to juggle
//undefined (there is no 3rd item because count starts with zero)
//W  (the 6th letter is "W")
//3 (number of things in array)
//11 (number of charactors in the string, including whitespaces)

